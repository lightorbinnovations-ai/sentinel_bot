name: The Void Sentinel

on:
  schedule:
    - cron: '*/15 * * * *' # Runs every 15 minutes
  workflow_dispatch: # Allows manual trigger

jobs:
  trade:
    runs-on: ubuntu-latest
    concurrency:
      group: void-sentinel-trading
      cancel-in-progress: false # We don't want to kill a live trade

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install Dependencies & Connectivity Test
        run: |
          python3 -m pip install --upgrade pip setuptools wheel
          python3 -m pip install -r requirements.txt
          echo "🌐 Testing connectivity to Deriv..."
          curl -v https://ws.derivws.com/websockets/v3?app_id=1089 || echo "⚠️ Connectivity to primary endpoint failed"

      - name: Run The Void Sentinel
        env:
          DERIV_TOKEN: ${{ secrets.DERIV_TOKEN }}
          GH_TOKEN: ${{ secrets.GH_TOKEN }}
          TELEGRAM_TOKEN: ${{ secrets.TELEGRAM_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          INITIAL_STAKE: ${{ vars.INITIAL_STAKE || '1.50' }}
          STOP_LOSS: ${{ vars.STOP_LOSS || '2.00' }}
          GHOST_THRESHOLD: ${{ vars.GHOST_THRESHOLD || '3' }}
          TAKE_PROFIT: ${{ vars.TAKE_PROFIT || '10.00' }}
        run: |
          # Use python3 explicitly and disable output buffering for real-time logs
          timeout 13m python3 -u sentinel.py || echo "Session completed or interrupted."
